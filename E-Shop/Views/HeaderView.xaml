<UserControl x:Class="E_Shop.Views.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:E_Shop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid Style="{StaticResource grid-primary}">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding Username}" Style="{StaticResource text-light}" HorizontalAlignment="Left"/>
            <StackPanel HorizontalAlignment="Right">
                <Button>
                    <Image Source="cart.png"/>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
